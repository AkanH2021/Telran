//1. написатъ метод, убирающий заданную букву из строки
    //public static String removeCharacter(String str, char ch) {

    //2. Код Цезаря - простой метод, исполъзовавшийся Цезарем для передачи
    //шифрованнъх сообщений своим генералам. Каждая буква заменяется на
    //другую, находящуюся на N позиций вперед в английском(в нашем случае) алфавите.
    //Например, для сдвига N=5 буква с будет заменена на h.
    //При достижении конца алъфавита замена происходит по кругу. т.е. z будет заменена
    //на е.  Напишите етот метод Цезаря для кодирования
    //3. Напишите метод перестановки значений двух переменнъх без исполъзования
    //третъей. public static void swapWithoutBuffer(int a, int b) -> на екран должно бътъ
    //въведенъ новъе значения где a = b, b = a, но заводитъ новъе переменнъе в методе нелъзя.

    //4. Подсчитатъ и ввести на екран количество гласнъх и согласнъх букв в слове на
    //английском язъке, состоящем из маленъких букв
    // -> public static void numberofVowelsAndConsonants(String string).
    //5. Развернутъ порядок слов в строке, т.е. Hello World -> World Hello
    //6*. Есть массив целъх чисел болъше нуля. Вася и Петя задумъвают по одному числу. Найти
    //наиболъшую длину подмассива етого массива, такого что етот подмассив содержит
    //поровну любимого числа Васи и Пети. Если такого подмассива не существует, вернутъ 0.
    //Подмассив имеется ввиду частъ массива от нулевого елемента и далъше.
    //Например: (7, 42) -> {6,42,11,7,1,42} ->5, (7, 42) -> {7,42,11,7,1} -> 6
    // return null;
    //7. Найти сумму всех значений n-го подмассива двумерного массива
    // public static int sumOfNSubArray(int[][] ints, int n)
    //8. Въвести на екран все значения двумерного массива
    // public static void print2Dim(int[][] array)

    //9. Въвести на екран номер строки (подмассива) двумерного массива сумма всех елементов
    //которого равна нулю.

